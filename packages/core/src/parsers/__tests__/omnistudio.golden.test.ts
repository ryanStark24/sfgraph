import { describe, it } from "vitest";

describe("omnistudio golden (gated: no real OmniStudio fixtures)", () => {
  it.skip("TODO: needs real OmniProcess metadata fixture from user", () => {});
  it.skip("TODO: needs real OmniDataTransform metadata fixture from user", () => {});
});
