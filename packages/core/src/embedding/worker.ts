/**
 * Reserved for the future piscina worker entry. The in-process queue in
 * `./queue.ts` does not require this file at runtime — kept as a placeholder
 * so the file layout matches the plan.
 */
export {};
