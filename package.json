{
  "name": "sfgraph-monorepo",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "models:refresh": "tsx scripts/refresh-vendored-model.ts",
    "vlocity:refresh": "tsx scripts/refresh-vlocity-registry.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@changesets/cli": "^2.27.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.2.4",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.4"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome",
      "better-sqlite3",
      "esbuild"
    ],
    "overrides": {
      "protobufjs@<7.2.5": "^7.2.5",
      "vite@<6.4.2": ">=6.4.2",
      "esbuild@<0.25.0": ">=0.25.0",
      "lodash@<4.18.0": ">=4.18.0"
    }
  }
}
